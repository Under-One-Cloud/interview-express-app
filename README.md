# Interview Express App

## Setup

You'll need to create a file called `cache.json` in the root file of this project. It should contain the following:

```json
[]
```

After that, run `index.js` to spin up the Express server.
